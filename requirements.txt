# Streamlit Community Cloud (uv/pip). Keep this lean.
# streamlit is preinstalled on Community Cloud — pin only if needed.
openai>=1.50,<2
python-dotenv>=1.0
minsearch>=0.0.12
numpy>=1.26,<3
# Psycopg 3 binary wheel (optional logging; Q&A works without Postgres)
psycopg-binary>=3.1
