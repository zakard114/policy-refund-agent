---
description: E: storage and Docker Compose conventions
alwaysApply: true
---

# E: drive and Docker Compose

- All data, caches, models, logs, DB volumes on **E:** — never `C:\Users\...`.
- Project root: `E:\IT_SPACES\AI\Projects\llm\policy-refund-agent\`
- Infra via `docker-compose.yaml`; bind mounts use `E:/IT_SPACES/AI/...` forward slashes.
- New terminal: `. .\scripts\use_e_drive.ps1` before `uv sync`.
